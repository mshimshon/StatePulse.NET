@page "/"

<h1>Hello, world!</h1>
@($"The Count is ")
<p>@($"{State.ActivePulsars}")</p>
<button class="btn btn-primary" @onclick=Click>Click me</button>
<button class="btn btn-primary" @onclick=ForceGC>Force GC</button>
<button class="btn btn-primary" @onclick=MassTest>Stress Tracker</button>
<button class="btn btn-primary" @onclick=StateHasChanged>Refresh Render</button>

@if (Visible)
{
    <CounterView />
}
@foreach (var item in _lastChecks)
{
    <p>@item.ToString()</p>
}    